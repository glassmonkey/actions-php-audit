version: '3'

services:
  app:
    build: .
    image: glassmonenkey/actions-php-audit
    environment:
      WORK_DIR: /app
    working_dir: /app
    volumes:
      - ./composer.lock:/app/composer.lock